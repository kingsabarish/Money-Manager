# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Tooling caches
.ruff_cache/
.mypy_cache/
.pytest_cache/

# Environments / secrets
.env

# Data (local SQLite dir created at runtime from the relative default URL).
# Anchored so it does not match the Android `data/` source package.
/data/
backend/data/
*.db
*.sqlite
*.sqlite3

# OS / editor
.DS_Store

# Android / Gradle
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/.idea/
android/captures/
*.apk
*.aab
*.keystore
!android/gradle/wrapper/gradle-wrapper.jar
